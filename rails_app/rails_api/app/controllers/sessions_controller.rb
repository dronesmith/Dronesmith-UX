class SessionsController < ApplicationController
    # skip_before_action :authenticate_user_from_token!

    def userdata
    #when react hit this path send user data to cloud url
      #collect: user.id, user.data  
      #call on send_method()
    end

    def send_to_url()
      #send post.request(url: )
    end
    
end